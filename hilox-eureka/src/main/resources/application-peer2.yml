server:
  port: 8002

eureka:
  instance:
    hostname: peer2
  client:
    service-url:
      defaultZone: http://peer1:8001/eureka/
  server:
    enable-self-preservation: false

spring:
  application:
    name: eureka-server

